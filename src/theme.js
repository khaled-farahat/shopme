import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    grayColor: "#707070",
    grayColor2: "#D8D8D8",
    whiteColor: "#FFFFFF",
    blackColor:{
      main: "#000000",
    } ,
  }
})

export default theme;
